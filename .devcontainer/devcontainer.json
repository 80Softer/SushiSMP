{
  "name": "SushiSMP Server",
  "image": "mcr.microsoft.com/devcontainers/java:17",
  
  // 📦 ここを追加すると、起動時に自動でインストールしてくれる！
  "features": {
    "ghcr.io/devcontainers/features/common-utils:1": {
      "installZsh": true,
      "configureZshAsDefaultShell": false
    }
  },

  // 🚀 ついでに「起動したら勝手にサーバーを動かす」設定
  "postStartCommand": "sudo apt-get update && sudo apt-get install -y screen && cd /workspaces/SushiSMP/server && bash start.sh"
}
